import { CardNumberPipePipe } from './card-number-pipe.pipe';

describe('CardNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CardNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
