import { DaysBetweenPipe } from './days-between.pipe';

describe('DaysBetweenPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysBetweenPipe();
    expect(pipe).toBeTruthy();
  });
});
