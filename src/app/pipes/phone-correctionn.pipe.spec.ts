import { PhoneCorrectionnPipe } from './phone-correctionn.pipe';

describe('PhoneCorrectionnPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneCorrectionnPipe();
    expect(pipe).toBeTruthy();
  });
});
