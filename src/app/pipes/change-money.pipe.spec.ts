import { ChangeMoneyPipe } from './change-money.pipe';

describe('ChangeMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
