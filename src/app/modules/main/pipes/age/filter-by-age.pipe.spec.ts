import { FilterByAgePipe } from './filter-by-age.pipe';

describe('FilterByAgePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAgePipe();
    expect(pipe).toBeTruthy();
  });
});
