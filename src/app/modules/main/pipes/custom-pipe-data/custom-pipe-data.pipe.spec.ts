import { CustomPipeDataPipe } from './custom-pipe-data.pipe';

describe('CustomPipeDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipeDataPipe();
    expect(pipe).toBeTruthy();
  });
});
