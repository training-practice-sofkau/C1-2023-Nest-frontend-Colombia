import { CustomPipePhonePipe } from './custom-pipe-phone.pipe';

describe('CustomPipePhonePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipePhonePipe();
    expect(pipe).toBeTruthy();
  });
});
