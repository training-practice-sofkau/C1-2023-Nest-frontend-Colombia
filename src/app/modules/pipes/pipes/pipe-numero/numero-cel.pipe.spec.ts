import { NumeroCelPipe } from './numero-cel.pipe';

describe('NumeroCelPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroCelPipe();
    expect(pipe).toBeTruthy();
  });
});
