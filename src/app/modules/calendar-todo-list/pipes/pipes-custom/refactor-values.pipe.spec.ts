import { RefactorValuesPipe } from './refactor-values.pipe';

describe('RefactorValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new RefactorValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
