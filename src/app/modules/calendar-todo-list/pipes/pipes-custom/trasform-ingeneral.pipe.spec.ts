import { TrasformIngeneralPipe } from './trasform-ingeneral.pipe';

describe('TrasformIngeneralPipe', () => {
  it('create an instance', () => {
    const pipe = new TrasformIngeneralPipe();
    expect(pipe).toBeTruthy();
  });
});
