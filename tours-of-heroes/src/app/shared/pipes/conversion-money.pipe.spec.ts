import { ConversionMoneyPipe } from './conversion-money.pipe';

describe('ConversionMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversionMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
